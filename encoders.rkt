#lang racket

#|
Now's not the time to implement these: that comes later!
-- Bain
|#

; Implementation of reverse of ASCIIHexDecode
;(define (hex-ascii-encode data))

; Implementation of reverse of ASCII85Decode
;(define (85-ascii-encode data))

; Implementation of reverse of FlateDecode
;(define (flate-encode data))

; Implementation of reverse of RunLengthDecode
;(define (run-length-encode data))

; Implementation of reverse of CCITTFaxDecode
;(define (ccitt-fax-encode data))

; Implementation of reverse of JBIG2Decode
;(define (jbig2-encode data))

; Implementation of reverse of JPXDecode
;(define (jpx-encode data))

; Implementation of reverse of Crypt (which is actually decrypt!)
;(define (crypt data))